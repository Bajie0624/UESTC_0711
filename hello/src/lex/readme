思想：
消除左递归
当有 | 时要进行if循环判断
当没有空串时，要进行出错判断
如果判断正确，就要进行下一个字符的判断，调用advance函数

note:产生变量名表、过程名表、出错处理
     语法错误：缺少符号、符号匹配出错、符号无定义或者重复定义、其他错误
     词法要把状态图贴进去、语法分析要把文法写进去（变量名表截图、过程名表截图、报错截图）
important：这里要注意，如果直接按照字符串进行匹配，到了最后，会发现像标识符、常数等，无法有效尽行匹配，
最好的结果还是直接按照种别进行匹配。千万要注意！